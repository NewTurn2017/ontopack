# Real test result — 2026-05-23

Repository: `ontopack`
Branch: `main`
Rust toolchain observed by runner: `v1.94.0`

## Summary

Both real-test lanes passed:

1. Default offline lane: `scripts/real-test.sh`
2. Optional real embedding lane: `RUN_REAL_EMBED=1 scripts/real-test.sh`

Result:

```text
Ontopack real test passed: realistic pack + CLI + MCP + viewer APIs + filter stress + open URL
```

## Default offline lane

Command:

```bash
scripts/real-test.sh
```

Observed stages:

```text
[1/10] build debug binaries
[2/10] seed realistic pack: /var/folders/6w/ryvjgm214g361w38k2x2dcch0000gn/T//ontopack-real-test.BCxik6
[3/10] process inbox and build index twice
[4/10] CLI real-user keyword searches
[5/10] viewer API filtered search, including >100 distractors
[6/10] viewer API facets/gallery/timeline/graph/note/related
[7/10] MCP stdio tools against realistic pack
[8/10] open URL smoke
[9/10] optional real embedding gate
skip real embedding download/runtime; set RUN_REAL_EMBED=1 to exercise BGE-M3 path
[10/10] real test summary
Ontopack real test passed: realistic pack + CLI + MCP + viewer APIs + filter stress + open URL
```

## Optional real embedding lane

Command:

```bash
RUN_REAL_EMBED=1 scripts/real-test.sh
```

Observed stages:

```text
[1/10] build debug binaries
[2/10] seed realistic pack: /var/folders/6w/ryvjgm214g361w38k2x2dcch0000gn/T//ontopack-real-test.wWK3FC
[3/10] process inbox and build index twice
[4/10] CLI real-user keyword searches
[5/10] viewer API filtered search, including >100 distractors
[6/10] viewer API facets/gallery/timeline/graph/note/related
[7/10] MCP stdio tools against realistic pack
[8/10] open URL smoke
[9/10] optional real embedding gate
[10/10] real test summary
Ontopack real test passed: realistic pack + CLI + MCP + viewer APIs + filter stress + open URL
```

Additional observation: first real-embed run downloaded BGE-M3/FastEmbed assets, including approximately `2.11 GiB` ONNX model data plus tokenizer/config files.

## Coverage confirmed

- Realistic pack creation and ingestion
- CLI keyword source-card search
- Incremental build path
- Viewer API search/ask/facets/gallery/timeline/graph/note/related
- Filter-stress regression with more than 100 distractors
- MCP stdio initialize/tools/search/timeline/ask
- `pack open --port 0 --no-browser --print-url`
- Optional `real-embed` build/embed/hybrid search path

## Remaining manual-only checks

The script validates API and URL behavior, not visual browser interaction. Manual browser QA remains useful for:

- Search/filter UI refresh behavior
- Ask/context-block readability
- Gallery card presentation
- Note detail, related, timeline, and graph panel usability
